<% if (title) { %>
	<h1><%= title %></h1>
	<%- messages() %>
	<% if (errors) { %>
		<ul class="notice">
	 <% errors.array().forEach(error => { %>
		 <li><%= error.msg %></li>
	<%  }) %>
	 </ul>
	<% } %>

		<li><a href="add-classification.ejs">Add New Classification</a></li>
		<li><a href="/inv/inventory/add">Add New Inventory</a></li>

	<% } else {
		res.redirect('/')
	} %>